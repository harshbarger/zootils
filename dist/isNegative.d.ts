/**
 * Indicates whether a number is negative.
 *
 * @param data
 * @returns
 *
 * @example
 * ```
 * isNegative(-2);    // true
 * isNegative(0);     // false
 * ```
 *
 * @category Math
 */
export declare function isNegative(data: number): boolean;
