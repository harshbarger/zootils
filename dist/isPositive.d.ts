/**
 * Indicates whether a number is positive.
 *
 * @param data
 *
 * @example
 * ```
 * isPositive(2);     // true
 * isPositive(0);     // false
 * ```
 *
 * @category Math
 */
export declare function isPositive(data: number): boolean;
