/**
 * Indicates whether a number is an odd integer.
 *
 * @param data
 * @returns
 *
 * @example
 * ```
 * isEven(3);        // true
 * isEven(3.1);      // false
 * ```
 *
 * @category Math
 */
export declare function isOdd(data: number): boolean;
